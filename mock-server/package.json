{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Mock server for development",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}